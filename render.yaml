services:
  - type: web
    name: dcup-web
    runtime: docker
    dockerContext: .
    dockerfilePath: ./Dockerfile
  
  - type: pserv
    name: parser
    runtime: docker
    dockerContext: ./svc_parser
    dockerfilePath: ./svc_parser/Dockerfile
